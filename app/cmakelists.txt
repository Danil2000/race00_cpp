cmake_minimum_required(VERSION 2.8)

project(app)

list(APPEND SOURCE_FILES game.cpp)
foreach(file ${SOURCE_FILES})
    list(APPEND SOURCE_PATHES src/${file})
endforeach()
add_library(src STATIC ${SOURCE_PATHES})